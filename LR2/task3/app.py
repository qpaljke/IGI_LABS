import square
import circle

a = int(input("a = "))

print(f"Square area = {square.area(a)}")
print(f"Square area = {square.perimeter(a)}")
print(f"Circle area = {circle.area(a)}")
print(f"Circle area = {circle.perimeter(a)}")